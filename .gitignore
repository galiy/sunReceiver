# runtime-данные poller'а
data/

# вся приватная папка .kilo (секреты, инвентарь связки ключей, SSH, токены) — не коммитить
.kilo/

# SSH-доступ к ПАК «Малина» (используется только при разработке) — не коммитить
.kilo/malina-ssh.json

# контекст по настройке/оптимизации ПАК «Малина» и сбора логов на 253 (приватный) — не коммитить
.kilo/malina-opt-context.md

# приватная инфраструктура sunReceiver (даталоггеры IP/SN, прод, SSH, деплой) — не коммитить
.kilo/AGENTS-private.md

# приватный worklog задачи «релиз» (прогресс, решения, договорённости между воркерами) — не коммитить
.kilo/release-worklog.md

# настройка HTTPS (Let's Encrypt + Apache reverse proxy на 253) (приватный) — не коммитить
docs/https-setup.md

# конфигурация электросчётчика DDS238 (подключение, учёт) — не коммитить
dds238.json

# основной конфигурационный файл (адреса, логин и пароль БД, устройства) — не коммитить
sunReceiver.json

# бинарники сборки
sunReceiver
sunReceiver-linux
solarman-probe
*.log
*.exe

# релизные артефакты сборки (make VERSION=... all) — не коммитить
dist/

# справочные материалы МАП (не закоммичены, лежат локально)
docs/map/

# рабочая папка загрузчика прошивок МИКРОАРТ (MAP2Gui + прошивки .map2) — вне git
map-loader/

# временные скрипты/контекст импорта исторических данных из Oracle — вне git
import_oracle/

# архив отчётов код-ревью (имя содержит дату) — вне git
archive/

# планы рефакторинга/задач — вне git (для других сессий)
plans/

# рабочие отчёты код-ревью (содержат ссылки на реальные идентификаторы) — вне git
review-*.md

.kilo/gh-token.txt
